const lowp float a7hue        = %hue%;      // modify to adjust hue (default: 0.0, use positive or negative values to shift hue in positive or negative direction)
const lowp float a7saturation = %saturation%;      // modify to adjust saturation (default: 1.0, desaturate: < 1.0, saturate: > 1.0)
const lowp float a7lightness  = %lightness%;      // modify to adjust lightness (default: 1.0, darker: <1.0, lighter: > 1.0)

